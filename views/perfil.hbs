{{> navbar}}

<div class="container text-center my-4 p-5">
<div><h1>Información del perfil</h1></div>
<div class="row align-items-center justify-content-around">
    <div class="col-4 border border-2 rounded border-dark">
<img src={{avatar}} alt="foto de usuario">
    </div>
    <div class="col-4  text-start border border-2 rounded border-dark">
        <h4>Mail: {{mail}}</h4>
        <h4>Usuario: {{usuario}}</h4>
        <h4>Edad: {{edad}}</h4>
        <h4>Dirección: {{direccion}}</h4>
        <h4>Teléfono: +{{prefijo}}-{{telefono}}</h4>
    </div>
</div>


</div>

{{> footer}}

